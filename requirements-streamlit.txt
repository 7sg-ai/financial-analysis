# Streamlit-specific requirements (no PySpark needed - calls API backend)
# Core Dependencies (no PySpark)
pyarrow==14.0.1
pandas==2.1.4

# CRUSOE Dependencies (replaced Azure-specific auth with generic HTTP client)
openai==1.12.0

# Web Framework
streamlit==1.29.0
plotly==5.17.0

# Utilities
python-dotenv==1.0.0
httpx==0.26.0
tenacity==8.2.3
requests==2.31.0

# Data Processing
numpy==1.26.3
tabulate==0.9.0
